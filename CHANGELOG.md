# 更新日志

本项目的所有重要改动都记在这里。

版本号是 主版本.次版本.修订号。
- 主版本：不兼容的大改动（比如换掉通信协议）
- 次版本：只有多个小更新才动
- 修订号：其余一切改动都走这里 —— 加功能、修 bug、调格式

比语义化版本更保守：一般的新增功能也只加修订号，攒够几个小更新再升次版本。

版本号不写进 commit title。**只给次版本及以上打 tag**（`v1.1.0`、`v1.2.0`、`v2.0.0`），
修订号那一层不打 —— 改动看 commit 就够了，不必在 tag 列表里占位。

前后端共用同一个版本号。两边不一致时，登录页会直接拦下并提示更新。

---

## [1.2.3] - 2026-07-27

### 新增
- 调试面板（网址后加 `?debug`）里直接看得到 `/who`：在线几台、每台是什么设备什么浏览器、
  页面哪个版本、多久没动静、从哪个网址进来的。每 5 秒刷新一次，有人进出时也顺手刷。
  以前只显示服务器推来的 `[{id,name}]`，看不出多出来的那台是谁

### 修复
- `/who` 里的 User-Agent 不再被截断到认不出浏览器 —— 浏览器名在 UA 最末尾，
  原来切到 120 字正好把 `Safari/604.1` 切掉，iPhone Safari 显示成「不知什么浏览器」

---

## [1.2.2] - 2026-07-27

### 修复
- 旧页面不再挂在在线名单里冒充一台设备。`/join` 现在带上前端版本，
  版本对不上的当场请出去，那边提示「请刷新页面」。
  在此之前，一个开了很久没刷新的标签页跑的是旧代码 —— 它不报设备号，顶不掉，
  又一直在取消息，超时也摘不掉，于是永远显示成第二台在线设备
- 被请出去的连接，即使那条消息随连接一起丢了，下次 `/poll` 也能拿到正确的原因；
  以前一律说成「被顶掉了」，会让人以为是别的设备抢了位置

### 新增
- `/who` 多返回三项：入口网址、浏览器、页面版本。名单里多出一台时，
  照着这三项就能认出是哪台设备上的哪个页面

---

## [1.2.1] - 2026-07-27

### 新增
- 前端显示当前版本：登录页按钮下面一行 `v1.2.1`，说明面板底部也带一个。
  两处都从 `index.html` 里的 `VERSION` 填，HTML 不再写死一遍

---

## [1.2.0] - 2026-07-27

服务端从「只认连接」改成「认设备」。这一版把 1.1.5、1.1.6 两次修订合并发布。

### 修复
- **手机上线时自己被数成两台设备。** 页面每次 join 都拿一个全新的连接 id，
  上一条连接要等 60 秒超时才消失，这中间顶栏就显示成「你 ×2」。
  现在前端在 `localStorage` 里存一个固定的设备编号（`heart-dev`），join 时带上，
  服务端认出同一台设备就当场把它的旧连接顶掉
- **从缓存里恢复的页面收不到消息。** iOS 上切走再切回来，页面常常是原样恢复的：
  `pagehide` 时已经报过离开、轮询也停了，页面看着还在其实早断了。
  补一个 `pageshow` 监听，从缓存恢复时重新接上

### 变更
- 被顶掉的连接 id 记 2 分钟。这期间它来 `/poll` 给一句 `evicted`、来 `/send` 给 409，
  都不再给 410 —— 410 会让页面重新 join，同一台设备开两个页面时两边会互相顶个没完
- `/who` 多一列「设备」，看得出哪几条连接来自同一台机器
- 顶栏在线名单里的自己，从「你」改成「名字（你）」，看得出这个页面是用哪个名字登录的

### 背景
症状是「手机 Safari 一上线就说有两个设备在线」，等一会儿又自己好了 ——
等的正是那 60 秒超时。根子在于服务端只按连接认人，认不出「还是刚才那台」。
`/leave` 的 `sendBeacon` 在 iOS 上切后台时并不可靠，指望不上。

---

## [1.1.4] - 2026-07-27

### 新增
- **第三道版本检查：重连时。** `welcome` 事件带着服务端版本，对不上就停掉轮询，
  顶栏显示「服务端已更新到 X，请刷新页面」

### 背景
登录时那道检查只在进门时跑一次。页面一直开着没关、服务端在这期间升级了的话，
旧页面会带着旧代码继续轮询——而旧代码可能根本处理不了新事件。症状是
「对方明明在线、消息也发了，我这边什么都没有」，但服务端日志和 `history/`
里一切正常，极难排查。1.1.1 修的 `handle` 缺失就是这种代码，任何在那之前
打开、之后没刷新过的页面都还是坏的。

判断依据：`hearts.json` 里对方今天的心跳计入了 `day.byName` 却没进 `stash`，
说明她点的时候服务端认为「有别人在线」，也就是消息确实广播出去了，是浏览器
这边没接住。

---

## [1.1.3] - 2026-07-27

### 变更
- 快捷短语从 3 个加到 11 个：早安、晚安、爱你、亲亲、吃了吗、我到家了、
  辛苦了、睡了吗
- **悬浮窗里仍然只显示最初那三个**。新增的都带 `extra` 类，
  靠 `.compact .chip.extra { display: none }` 挡掉 —— 悬浮窗只有 200px 宽，
  短语一多就会把窗口撑开

---

## [1.1.2] - 2026-07-27

### 变更
- 聊天记录的时间改成给人看的 `yymmdd 时:分:秒`（`260727 14:26:01`）
  - 记录面板本来只显示 `时:分:秒`，日期靠下拉框区分，现在直接带上日期
  - `/history` 在原有的 `t` 之外额外返回一个转好的 `time` 字段
- `history/` 文件里**仍然存毫秒时间戳**，格式没变，已有记录不用迁移。
  时间戳是无歧义的绝对时刻，排序、算时长、换时区都靠它；可读时间只在
  出口处转一次

---

## [1.1.1] - 2026-07-27

两个既有 bug，都不是 1.1.0 引入的，是加聊天记录时顺带查出来的。

### 修复
- **前端收不到任何服务端消息**。`index.html` 里调了 `handle(m.ev, m.data)`
  来分发事件，但这个函数整个文件里只有调用、没有定义。异常被轮询的
  `try/catch` 吞掉，当成断线重试，于是对方的心跳、纸条、在线名单、同频特效、
  离线暂存回放全部收不到，底部计数永远是 0。发送方向一直是好的
  （`/send` 照常计数存盘），所以统计在涨，不容易发现。
  补上分发函数，处理 `welcome` / `presence` / `tap` / `sync` / `note` /
  `stash` / `pong` / `evicted` 八个事件。
  - 顺带：`alive()` 恢复连接时没有重画顶栏，文字会永久卡在「断线了，重连中」；
    小圆点也从来没加过 `.on`，一直是灰的。都补上了
  - 这个 bug 还在静默丢数据：服务端是「先从 stash 删掉、再推给客户端」，
    客户端收不到就等于每次登录都丢一批攒下的心跳和纸条
- **连接数满 8 条时服务器会卡死**。`admit()` 里是
  `while (clients.size >= MAX_CLIENTS) evict(...)`，而 `evict()` 只是发个
  通知、把真正的移除放进 `setTimeout(..., 100)`。`size` 当场不变，循环
  就地转死，同时无限堆积定时器。实测第 9 条连接进来后 CPU 打满、内存
  几秒涨到 3 GB、整个服务端失去响应。改成 `evict()` 当场从名单里摘掉。
  现在连开 15 条连接，CPU 0.2%、内存 48 MB、连接数稳定压在 8

### 说明
- README 里「房间最多 4 个连接」与代码不符（`MAX_CLIENTS = 8`），已更正

---

## [1.1.0] - 2026-07-27

### 新增
- **聊天记录**：纸条不再是阅后即焚，服务端会留存下来。一天一个文件，
  存在 `history/` 下，文件名就是日期（`history/20260727`），
  一行一条 JSON。追加写，服务端不读回内存
- **记录面板**：`?debug` 下右下角新增面板，选日期看当天的纸条，
  按「时间 名字 内容」列出，自己的名字标粉色。看今天的每 5 秒自动跟进，
  翻旧日期时不刷新
- `GET /history?key=` 取日期清单，`&date=20260727` 取某天的记录。
  date 只认 8 位数字，挡掉路径穿越
- `/who` 多返回一个「聊天记录天数」

### 说明
- `history/` 含纸条原文，已加进 `.gitignore`，不进仓库
- `/reset` 只清统计，不动聊天记录；要删记录直接删 `history/` 下的文件

---

## [1.0.0] - 2026-07-23

第一个稳定版。经过一路排查，通信最终定在 HTTP 长轮询——它能稳定穿过
Cloudflare 隧道，不像 SSE 会被缓冲、也不像 WebSocket 会被拦。

### 核心
- 悬浮的大爱心，点一下两边一起冒小爱心；你的是粉色，对方的是青色
- 点击时爱心先缩后弹的动效
- 按住连发，空格键同理
- **同频**：两人在 3 秒内先后点击，两边一起爆开金色特效
- **离线暂存**：对方不在时点的心跳和纸条都攒着，等 TA 上线一次放出
- **小纸条**：三个快捷词加自由输入，飘过两边屏幕，不留记录
- 代码现场合成的音效，可开关
- 两人都在线时的「同在」计时
- 今日 / 累计 / 分人 / 同频次数统计，存盘不丢

### 界面
- 顶栏显示在线人数和名单，多设备用 ×N 标注
- 说明面板（? 按钮），首次进入自动展开一次
- 置顶悬浮窗（Document Picture-in-Picture），极简模式只留一颗心，
  鼠标移上去才浮出快捷条
- 调试面板：网址加 `?debug` 打开

### 通信与稳定性
- HTTP 长轮询：`/join` 拿 id，`/poll` 取消息，`/send` 发消息，
  `/leave` 关页面时立刻下线
- 按名字认人，自己开多个标签页不会被算成两个人
- 心跳超时清理离线连接，六秒宽限期避免抖动误判
- 关页面时用 sendBeacon 主动下线，不用等超时

### 安全
- 房间口令，前后端都校验，时间恒定比较
- 每连接限流，两人在同一台机器上不会共用配额
- 连接数上限，满了踢最久没动静的

### 运维
- 前后端版本号一致性检查
- `/who` 诊断接口，`/reset` 清零接口
- 零依赖，只用 Node.js 内置模块

---

## 早期迭代（未发布，记录演进）

- SSE 版：最初用 Server-Sent Events，本地正常，但经 Cloudflare 隧道
  会被缓冲，消息一阵一阵到、延迟极高
- WebSocket 版：手写握手和帧编解码，本地延迟降到个位数毫秒，但隧道
  不转发 upgrade 请求，握手直接失败（1006）
- 一度做成 WebSocket + 长轮询双通道自动降级，最终按需求砍掉 WebSocket，
  只保留长轮询
