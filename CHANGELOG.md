# 更新日志

本项目的所有重要改动都记在这里。

版本号遵循 [语义化版本](https://semver.org/lang/zh-CN/)：主版本.次版本.修订号。
- 主版本：不兼容的大改动（比如换掉通信协议）
- 次版本：新增功能，向下兼容
- 修订号：修 bug，不加功能

前后端共用同一个版本号。两边不一致时，登录页会直接拦下并提示更新。

---

## [1.0.0] - 2026-07-23

第一个稳定版。经过一路排查，通信最终定在 HTTP 长轮询——它能稳定穿过
Cloudflare 隧道，不像 SSE 会被缓冲、也不像 WebSocket 会被拦。

### 核心
- 悬浮的大爱心，点一下两边一起冒小爱心；你的是粉色，对方的是青色
- 点击时爱心先缩后弹的动效
- 按住连发，空格键同理
- **同频**：两人在 3 秒内先后点击，两边一起爆开金色特效
- **离线暂存**：对方不在时点的心跳和纸条都攒着，等 TA 上线一次放出
- **小纸条**：三个快捷词加自由输入，飘过两边屏幕，不留记录
- 代码现场合成的音效，可开关
- 两人都在线时的「同在」计时
- 今日 / 累计 / 分人 / 同频次数统计，存盘不丢

### 界面
- 顶栏显示在线人数和名单，多设备用 ×N 标注
- 说明面板（? 按钮），首次进入自动展开一次
- 置顶悬浮窗（Document Picture-in-Picture），极简模式只留一颗心，
  鼠标移上去才浮出快捷条
- 调试面板：网址加 `?debug` 打开

### 通信与稳定性
- HTTP 长轮询：`/join` 拿 id，`/poll` 取消息，`/send` 发消息，
  `/leave` 关页面时立刻下线
- 按名字认人，自己开多个标签页不会被算成两个人
- 心跳超时清理离线连接，六秒宽限期避免抖动误判
- 关页面时用 sendBeacon 主动下线，不用等超时

### 安全
- 房间口令，前后端都校验，时间恒定比较
- 每连接限流，两人在同一台机器上不会共用配额
- 连接数上限，满了踢最久没动静的

### 运维
- 前后端版本号一致性检查
- `/who` 诊断接口，`/reset` 清零接口
- 零依赖，只用 Node.js 内置模块

---

## 早期迭代（未发布，记录演进）

- SSE 版：最初用 Server-Sent Events，本地正常，但经 Cloudflare 隧道
  会被缓冲，消息一阵一阵到、延迟极高
- WebSocket 版：手写握手和帧编解码，本地延迟降到个位数毫秒，但隧道
  不转发 upgrade 请求，握手直接失败（1006）
- 一度做成 WebSocket + 长轮询双通道自动降级，最终按需求砍掉 WebSocket，
  只保留长轮询
